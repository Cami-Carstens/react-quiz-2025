const Header = () => {
    return (
        <header style = {{backgroundColor: "#001833", color: "#fff", padding: "1rem", width: "100%", marginBottom: "1rem", textAlign: "center"}}>
            <h1>Practice React Quiz</h1>
        </header>

    );
};

export default Header;